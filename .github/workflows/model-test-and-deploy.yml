name: Model Validate and Build
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  validate-and-build:
    uses: CBIIT/bento-workflows/.github/workflows/model-test-and-deploy.yml@v1.1.4.1
    with:
      model_files: |
        ctdc_model_file.yaml ctdc_model_properties_file.yaml
      model_prefix: ctdc-model
      event_name: ${{ github.event_name }}
